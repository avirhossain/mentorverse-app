rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Public read-only access to sessions
    match /sessions/{sessionId} {
      allow read: if true;    // get + list allowed for everyone (even without login)
      allow write: if false;  // nobody can create/update/delete from client
    }
  }
}
